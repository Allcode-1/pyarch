services:
  migrate:
    build: .
    env_file: .env
    command: uv run alembic upgrade head

  api:
    build: .
    env_file: .env
    ports:
      - "8000:8000"
    depends_on:
      migrate:
        condition: service_completed_successfully
